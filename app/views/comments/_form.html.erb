<%# message, comment %>

<%= form_for comment, :url=>message_comment_path(message,comment), :method=>:patch, :remote=>true do |form| %>

  <%= form.text_field :content %>
<!-- text_field_tag('comment','content',@comment.content) -->

<% end %>


